[pytest]
# Put the project root on sys.path so `pytest` finds the `pipeline` package
# without an install step. Without this, bare `pytest tests/` fails with
# ModuleNotFoundError while `python -m pytest` (which adds cwd itself)
# succeeds -- a difference that is easy to trip over.
pythonpath = .
testpaths = tests
addopts = -q
